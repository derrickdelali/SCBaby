<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scbaby">
<uses-permission android:name="android.permission.RECORD_AUDIO">
</uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SCBaby">
        <activity android:name=".Feedback_Message"
            android:theme="@style/AppTheme.FullScreen"></activity>
        <activity
            android:name=".More_Information"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".Cry_Recognition"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".Record"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".Disclaimer"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".Register"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>